[project]
name = "kafka-order-assignment"
version = "0.1.0"
description = "Resilient Kafka Order Processing Pipeline with Avro, retries, DLQ routing, and running aggregation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.15.0",
    "fastavro>=1.12.2",
    "PyYAML>=6.0.3",
]
