[project]
name = "kafka-order-assignment"
version = "0.1.0"
description = "Basic Python Kafka producer and consumer assignment"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.15.0",
]
